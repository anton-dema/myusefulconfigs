[upsadmin]
# Administrative User
password = stronxpasswdxxxxx
# Allow changing values of certain variables in the UPS.
actions = SET
# Allow setting the Forced Shutdown flag in the UPS.
actions = FSD
# Allow all instant commands
instcmds = ALL
upsmon primary
